# HerbWire V2 Milestone 0 does not require runtime environment variables.
#
# When a runtime stack is introduced in a later milestone, document each
# variable here with a safe placeholder and a short explanation.
#
# Real values belong only in an untracked .env file or in an approved
# provider secret store.
#
# No Heroku credential is configured today.
# No Zyte credential is configured today.
